<div class="background">
  <p class="title">Lists</p>
  <%= image_tag "banner.jpeg", height: 400, style: "width: 100%" %>
</div>
<div class="container">
  <div class="row">
    <% @lists.each do |list|%>
      <%= render "card", list: list %>
    <% end %>
  </div>
</div>
